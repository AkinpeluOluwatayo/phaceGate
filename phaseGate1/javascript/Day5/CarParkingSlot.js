//declare function
// declare number of parking slots
//collect select of slot
// use for loop to check the slot choice
// use if statement to compare the slot choice to boolean
//print out function




function CarParkingSlot (num1);

let[] slots = new let[5];

console.log("Pick car park slot number '1 - 5' ");

let unOccupied = true;

for (let count = 0; count <= slots.length; count++){
	if (slots[count] && unOccupied){

	     console.log("Car slot is free");

	}else{
	     console.log("Car slot is occupied");
	     }

}

console.log(CarParkingSlot);